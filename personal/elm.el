;;; elm.el --- Emacs Prelude: Elm tweaks
;;
;;; Commentary:
;; Personal tweaks of the Elm mode

;;; Code:

;; Force tags to be generated when file is saved
(setq elm-tags-on-save t)

;; Force elm-format to be invoked on every file save
(setq elm-format-on-save t)

;;; elm.el ends here
